[package]
name = "op-cache"
version = "2.0.0"
edition = "2024"
description = "Fast cache for 1Password CLI reads"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
libc = "0.2"

[profile.release]
strip = true
lto = true
