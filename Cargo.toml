[package]
name = "op-cache"
version = "1.0.0"
edition = "2024"
description = "Fast cache for 1Password CLI reads"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"

[profile.release]
strip = true
lto = true
